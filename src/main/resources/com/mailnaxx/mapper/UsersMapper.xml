<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mailnaxx.mapper.UsersMapper">
    <select id="selectAll" resultType="com.mailnaxx.entity.Users">
        SELECT * FROM users
    </select>

    <insert id="insert" parameterType="com.mailnaxx.entity.Users">
        INSERT INTO users
        (
            user_id,
            user_name,
            user_name_kana,
            hire_date,
            affiliation_id,
            role_class,
            is_sales,
            birth_date,
            post_code,
            address,
            phone_number,
            email_address,
            password,
            pass_changed_date,
            old_password,
            last_login_date,
            is_deleted,
            created_by,
            created_at,
            updated_by,
            updated_at
        )
        VALUES
        (
            #{user_id},
            #{user_name},
            #{user_name_kana},
            TO_DATE(#{hire_date},'YYYYMMDD'),
            #{affiliation_id},
            #{role_class},
            #{is_sales},
            TO_DATE(#{birth_date},'YYYYMMDD'),
            #{post_code},
            #{address},
            #{phone_number},
            #{email_address},
            #{password},
            #{pass_changed_date},
            #{old_password},
            #{last_login_date},
            #{is_deleted},
            #{created_by},
            CURRENT_TIMESTAMP,
            #{updated_by},
            #{updated_at}

        )
    </insert>
</mapper>